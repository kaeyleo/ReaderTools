/**
 * readerTools
 * 
 * @Author liaokeyu
 * @date   2017-05-28
 * 
 */

var readerTools = (function(){

    'use strict';

    var defaults = {};

    var init = function(el, opt) {
        
    };

    /**
     * Helper
     */
    function extend(target, source) {

    }

    return {
        init: init
    }
})();

readerTools.init();